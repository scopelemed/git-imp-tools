# Hooks

Este diretório contém hooks versionados. Configure com:

- PowerShell: `git config core.hooksPath .githooks`
- bash: `git config core.hooksPath .githooks`

Hook atual:
- `commit-msg`: valida o padrão de mensagem de commit.
